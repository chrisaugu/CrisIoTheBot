const KEY = {
	UP: 38,
	DOWN: 40,
	W: 87,
	S: 83
};

// listen to the key down event
$(document).keydown(function(e) {
	switch (e.which) {
		case KEY.UP:
			break;
		case KEY.DOWN:
			break;
		case KEY.S:
			break;
		case KEY.W:
			break;
	}
})